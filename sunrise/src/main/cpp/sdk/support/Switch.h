#ifndef SUNRISE_SWITCH_H
#define SUNRISE_SWITCH_H

class Switch {
public:
    static bool isDebug;

    //开关mopub test使用
    static bool open_mopub;
    static bool mopub_enable;

    //开关pangle test使用
    static bool open_pangle;
    static bool pangle_enagle;

    //开关ironSource test使用
    static bool open_ironsource;
    static bool ironsource_enable;

    //开关offer test使用
    static bool open_offer;
    static bool offer_enable;
};


#endif //SUNRISE_SWITCH_H
